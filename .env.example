# Python: environment variables or root .env (never committed).
# Next.js: root .env.local or environment variables.
LLM_API_KEY=
EMBEDDING_API_KEY=
# Both runtimes must point at the same config. Default: config.json
# INTERVIEW_CONFIG=config.local.json
