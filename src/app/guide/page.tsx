import Link from "next/link";
import { ArrowLeft, FilePlus2, Sparkles, Search } from "lucide-react";
import { Shell } from "@/components/shell";
export const metadata = { title: "整理指南" };
export default function Guide() { return <Shell><div className="mx-auto max-w-3xl"><Link href="/" className="inline-flex items-center gap-2 text-xs text-stone-500"><ArrowLeft size={15}/>返回资料库</Link><h1 className="mb-4 mt-8 text-3xl font-semibold">给经历一个归处</h1><p className="mb-10 text-sm leading-7 text-stone-500">第一版通过本地文件整理资料。保留原文，让总结和搜索成为理解它的另一种方式。</p><div className="space-y-5">{[{ icon: FilePlus2, title: "01 / 保存原始面经", text: "每篇面经保存成一个 UTF-8 编码的 .md 或 .txt 文件，放入项目的 data/raw 文件夹。文件名可以自由命名，原文内容无需使用固定模板。个人资料建议放入 data/raw/private，避免被加入版本管理。" }, { icon: Sparkles, title: "02 / 生成整理结果", text: "按 README 配置彼此独立的 LLM 与 Embedding 服务，再运行本地处理脚本。它会生成总结、标签与题目，并建立搜索索引。默认演示模式不会调用云服务，也不代表真实模型效果。" }, { icon: Search, title: "03 / 阅读与搜索", text: "重新构建并启动网站后即可阅读。关键词适合查找精确的公司与技术名词；语义搜索适合用自然语言描述问题。修改或删除原文件后，重新处理和构建即可同步更新。" }].map(({icon: Icon,title,text}) => <section key={title} className="rounded-2xl border border-[#dfe6d8] bg-white p-7"><Icon size={22} className="mb-4 text-[#6b8a61]"/><h2 className="mb-3 text-lg font-medium">{title}</h2><p className="text-sm leading-8 text-stone-500">{text}</p></section>)}</div><p className="mt-7 text-xs leading-6 text-stone-400">AI 整理可能存在遗漏。题目附带原文依据，完整原文始终保留，方便核对。</p></div></Shell>; }
